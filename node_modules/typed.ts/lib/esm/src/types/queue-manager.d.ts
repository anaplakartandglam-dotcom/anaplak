import { Resetter } from '../utils/resetter.js';
import { Queue } from './queue.js';
import { QueueItem } from './queue-item.js';
export declare class QueueManager<const NamedParts extends string[]> {
    private readonly _name;
    private readonly _namedParts;
    private readonly _queues;
    constructor(name: string, resetter: Resetter, namedParts: NamedParts);
    get(part: NamedParts[number]): Queue;
    clear(): void;
    add(item: QueueItem): void;
    resetIndices(): void;
}
